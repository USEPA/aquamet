
#' @export
calcInvertMets <- function(inCts, inTaxa=NULL, ct='FINAL_CT', dist='IS_DISTINCT', taxalevel='wSA'){
  # This function is a wrapper for functions that calculate subsets of metrics and combines them
  
}